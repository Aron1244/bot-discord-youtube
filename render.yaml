services:
  - type: worker
    name: discord-bot
    runtime: python
    buildCommand: |
      pip install -r requirements.txt
      apt-get update
      apt-get install -y ffmpeg
    startCommand: python bot_yt.py